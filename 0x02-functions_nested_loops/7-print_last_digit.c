#include "main.h"
/**
* print_last_digit - Prints the last digit
* of a number.
*
* @z: input number as an integer.
*
* Return: last digit.
*/
int print_last_digit(int z);
{
int x = 10, z, d;
d = z % x;
if (d < 0)
{
putchar(-d + 48)
return (-d);
}
else
{
putchar(-d + 48)
return (d);
}
}
